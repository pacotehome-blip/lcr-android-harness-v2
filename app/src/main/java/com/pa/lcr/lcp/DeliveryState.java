
package com.pa.lcr.lcp;

public enum DeliveryState {
    DISCONNECTED,
    CONNECTED,
    IDLE,
    PRESTART,
    STARTING,
    RUNNING_FLOWING,
    RUNNING_PAUSED,
    ENDING,
    ENDED,
    ERROR
}
